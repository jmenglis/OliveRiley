import React, { Component, PropTypes } from 'react'
import { Link } from 'react-router'
import request from 'superagent'

export default class Checkout extends Component {
  render() {
    return (
      <div>
        <h1>Here is where the checkout process goes</h1>
      </div>
    );
  }
}